const demo = Array.from({length:50},(_,i)=>({rank:i+1,title:i===0?'Twenty3rd Hot 50 — Coming Soon':`Chart Entry ${i+1}`,artist:i===0?'Kenya’s artists, producers & creators':'Twenty3rd Catalogue',movement:i<3?null:(i%5)-2,score:100-i*1.7}))

export default function Home(){return <main className="shell"><header className="top"><div className="brand">TWENTY3RD MUSIC GROUP</div><div className="nav">CHARTS &nbsp; PRODUCERS &nbsp; RISING &nbsp; VIDEOS</div></header><section className="hero"><div className="eyebrow">Kenya · Weekly Music Chart</div><h1>23rd<br/>HOT 50</h1><p>The independent weekly ranking of Kenya’s most influential songs. Built around transparent, measurable performance data — published every Friday.</p><div className="edition"><span className="status">FIRST EDITION — BUILDING</span><span>Twenty3rd Music Group</span></div></section><section className="chart">{demo.map(x=><article className="row" key={x.rank}><div className="rank">{String(x.rank).padStart(2,'0')}</div><div className="song"><div className="cover"/><div><div className="title">{x.title}{x.rank===1&&<span className="new">NEW</span>}</div><div className="artist">{x.artist}</div></div></div><div className="move">{x.movement===null?'—':x.movement>0?`↑ ${x.movement}`:x.movement<0?`↓ ${Math.abs(x.movement)}`:'—'}</div><div className="score">{x.score.toFixed(1)}</div></article>)}</section><footer className="footer">© 2026 Twenty3rd Music Group · 23rd Hot 50 · Rankings are generated from validated performance data. Methodology and source disclosures will be published with the first official chart.</footer></main>}
